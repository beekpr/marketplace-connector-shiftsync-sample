buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        maven { url 'https://s3-eu-west-1.amazonaws.com/beekeeper-marketplace-sdk/maven2/release/' }
    }
    dependencies {
        classpath "io.beekeeper:connector-plugin:${sdkVersion}"
    }
}

